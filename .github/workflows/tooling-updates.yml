name: tooling-updates

on:
  schedule:
    - cron: '0 4 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  tooling-updates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v6

      - name: Run tooling update check
        run: ./gradlew toolingUpdates
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Open or update the Tooling updates issue
        shell: bash
        run: |
          set -euo pipefail

          report=build/tooling-updates/report.txt
          issue_title="Tooling updates"
          owner="${{ github.repository_owner }}"

          updates="$(grep -v '^No tooling updates available' "$report" 2>/dev/null || true)"

          if [ -n "$updates" ]; then
            body="cc @$owner"$'\n\n'"\`\`\`text"$'\n'"$updates"$'\n'"\`\`\`"
          else
            body="No tooling updates available."
          fi

          existing="$(gh issue list --repo "${{ github.repository }}" \
            --search "in:title \"$issue_title\" is:issue is:open" \
            --json number --jq '.[0].number // empty')"

          if [ -n "$existing" ]; then
            gh issue edit "$existing" --repo "${{ github.repository }}" --body "$body"
            echo "Updated issue #$existing"
            issue_number="$existing"
          else
            gh issue create --repo "${{ github.repository }}" --title "$issue_title" --body "$body"
            echo "Created new issue"
            issue_number="$(gh issue list --repo "${{ github.repository }}" \
              --search "in:title \"$issue_title\" is:issue is:open" \
              --json number --jq '.[0].number // empty')"
          fi

          if [ -n "$updates" ] && [ -n "$issue_number" ]; then
            previous="$(gh api "repos/${{ github.repository }}/issues/$issue_number/comments" \
              --jq '[.[] | select(.user.login == "github-actions[bot]"
              and (.body | contains("cc @")))][0].id // empty')"
            if [ -n "$previous" ]; then
              gh api "repos/${{ github.repository }}/issues/comments/$previous" --method DELETE --silent
            fi
            gh issue comment "$issue_number" --repo "${{ github.repository }}" --body "$body"
            echo "Commented on issue #$issue_number"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
