---
description: Set up the tooling MCP servers (GitHub core; Steroid optional for IDEA)
---

Set up this repo's tooling MCP servers for the current user.

Follow the `setup-agent-tools` skill: detect the current state, wire the **GitHub** MCP (core), optionally the
**Steroid** MCP (IntelliJ IDEA users only), verify with `opencode mcp list`, and tell the user to restart OpenCode.
Confirm with the user before touching anything outside this repo.
