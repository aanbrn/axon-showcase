# Axon Framework 4 → 5 Migration — Learnings

Append-only. One dated entry per fired Learning Trigger — see the canonical list and the governing
rule (*capture any surprise that helps the next recipe run more smoothly*) in
`references/recipes/DEFAULT.md § Learning Triggers`. Read on demand — `progress.md` is the source of
truth for state.

```
## YYYY-MM-DD — <one-line headline>
**Recipe:** axon4to5-<component>      (or `—` for orchestrator / debug-loop learnings not bound to a recipe)
**Trigger:** <tag from § Learning Triggers, or other:<short-tag>>
**Where:** `<fqn>` | `<file:line>` | `<module>` | project-wide      (best-effort location — NEVER blank)
**Surprise:** what was unexpected.
**Resolution:** what was done. Link to commit `<sha>` if applicable.
```

---
