# Helm chart names whose MAJOR version updates are suppressed from the helmUpdates report.
# Minor and patch updates for these charts are still reported.
# A major chart bump carries a new preconfigured image tag that diverges from the
# test-surface *-image-tag pins (docker-compose/Testcontainers), so it is a coordinated
# migration, not an automatic update — see showcase/quality/infra-image-versions.
bitnami-postgresql
bitnami-kafka
bitnami-opensearch