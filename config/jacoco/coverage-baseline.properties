# Minimum instruction coverage enforced per module by the jacocoTestCoverageVerification gate in check.
# Measured across unit + component + integration tests; modules can opt out via coverage.gate.enabled=false.
coverage.instruction.minimum = 0.80
