{{- /*
Renders any additional manifests supplied via extraDeploy.
Each entry is templated and emitted as a separate document.
*/ -}}
{{- $context := $ }}
{{- range .Values.extraDeploy }}
---
{{ include "common.tplvalues.render" (dict "value" . "context" $context) }}
{{- end }}
